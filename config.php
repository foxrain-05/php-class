<?php

// config.php - 모든 페이지에 포함

// 데이터베이스 전역 설정
$host = 'localhost';
$port = 3307;
$dbname = 'todo_app';
$username = 'root';
$password = '1234';

// 전역 DB 설정 배열
$dbConfig = array(
    'host' => $host,
    'port' => $port,
    'dbname' => $dbname,
    'username' => $username,
    'password' => $password,
);

?>